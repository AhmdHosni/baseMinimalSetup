
# ============================================================================
# Scripts Configuration File
# ============================================================================
# Format: script_name.sh|run_as_root|description
# 
# Parameters:
#   - script_name.sh: Name of the script in the scripts/ folder
#   - run_as_root: true (requires sudo) or false (user-level)
#   - description: Human-readable description shown during execution
#
# Notes:
#   - Scripts execute in the order listed
#   - Lines starting with # are comments
#   - Scripts marked with (requires reboot) may trigger system reboot
#   - All scripts should be in the scripts/ folder
# ============================================================================

# ----------------------------------------------------------------------------
# SYSTEM-LEVEL SCRIPTS (Root/Sudo Required) no lib_functions
# ----------------------------------------------------------------------------



# ----------------------------------------------------------------------------
# USER-LEVEL SCRIPTS (No sudo required) with lib-functions
# ----------------------------------------------------------------------------
install-hyprland.sh|false|Install hyprland to base 
# install-paru.sh|false|Install Paru AUR Package Manager 
install-pywal16.sh|false|Installing Pywal from AUR  # (requires restart)
copy-configs.sh|false|Adding config files 

# ============================================================================
# NOTES ON SPECIFIC SCRIPTS
# ============================================================================
#
# copy_zsh_config_files.sh:
#   - May require reboot to apply shell changes
#   - Ensure your Zsh config is ready before running
#
# install_gonme.sh:
#   - GNOME installation typically requires reboot
#   - Large download and installation time expected
#
# disable_sudo_as_admin.sh:
#   - Only runs on Debian-based systems
#   - Modifies sudoers configuration
#
# ============================================================================
